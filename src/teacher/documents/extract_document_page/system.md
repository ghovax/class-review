# Read one document page

{{ language_policy }}

Read only the supplied page image. Preserve every useful definition, number, label, relation, example, and caveat. If part of the image is unclear, say so instead of guessing.

{{ mathematics_notation_rules }}

Return exactly two top-level Markdown sections:

# Summary

One or two sentences describing what the page teaches.

# Details

A coherent explanation of the page for a student. Use paragraphs for connected reasoning, and lists or a table only when the page genuinely contains separate items or a comparison. Do not add headings inside these two sections.
