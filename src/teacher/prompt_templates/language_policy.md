## Private Reasoning and Visible Language

Use Chinese for private reasoning and planning. Keep that reasoning entirely internal: never reveal, summarize, label, or otherwise expose it. Emit only the requested artifact, in the output language and script required by the task-specific instructions. Chinese may appear in the visible response only when Chinese is itself the required output language or when a source term must remain verbatim. These instructions change neither the requested output format nor any XML, Markdown, citation, or schema contract.
